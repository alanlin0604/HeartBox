# 工作交接 — 2026-05-10

> 這份取代 [`工作交接-2026-05-06.md`](工作交接-2026-05-06.md) 作為當前主交接文件。
> Play Store 細節仍維持在 [`play-store-todo-跨電腦.md`](play-store-todo-跨電腦.md) 和 [`play-store-上架流程剩餘.md`](play-store-上架流程剩餘.md)。

---

## 📌 目前狀態（origin/main）

最新 commit: `c35b93d`

**最近一輪 session（從 5/9 起的 rounds，從新到舊）：**
```
c35b93d fix(mobile): light theme default + adaptive icon safe zone
d336ee0 docs: cross-machine checklist for remaining Play Store steps
b4ea068 fix(layout): add language picker to mobile menu
37755a1 fix(a11y,frontend): global focus ring + contrast + cache cross-user leak
bf674ed fix(backend): P1 sweep — auth gating, pagination tie-break, friend activity N+1
c9425b7 fix(round4): chat atomicity + WS token refresh + per-user tz weekly summary
7f8250b fix(frontend,ios): SW cache busting on deploy + iOS missing usage strings
97185ce fix(backend): OAuth 2FA bypass + transaction safety + Celery resilience
82c6fcf fix(frontend,a11y,i18n): notif type drift + search labels + 27 i18n keys
7e8c34d fix(backend): friends error envelope + HabitAnalytics N+1 / contract drift
884e81f docs: Play Store remaining-steps checklist (with screenshot specs)
... (更早的請看 git log)
```

整體 health：
- Frontend lint: **0 errors** + 64 warnings (react-hooks/exhaustive-deps Bucket B intentional)
- Frontend tests: **78/78** vitest passing
- Backend tests: **113/113** Django passing (CI verified)
- Frontend bundle build: green
- 三個 locale (zh-TW / en / ja) 同步在 ~1496 keys

---

## ✅ 這次 session 已完成

### 4 輪 audit 全面審查（各派 3 個 Explore agent 並行掃 P0/P1/P2）

**Round 1（a11y / perf / cross-cutting）**
- `/habits` crash（DRF pagination shape mismatch）
- `/friends` 排行榜 i18n key path 錯誤 + 30 個 missing key
- 9 處紫色 / 綠色 brand drift（dashboard / sleep / friends / dashboard widgets / habit form / tag input）
- `friends_views.py` error envelope `{'error': ...}` → `{'detail', 'code'}`
- `HabitAnalyticsView` N+1 + contract drift（chart 一直顯示空，frontend 期待 `res.data.analytics[].avg_mood_completed` 但 backend 回 bare list with `_when_completed` infix）
- `NotificationBell` 補 7 個 missing notification type mapping
- `NoteCard` Link aria-label + focus-visible
- `SearchFilterPanel` 6 個 range 輸入加 aria-labelledby

**Round 2（pagination shape / fetch race / colors / tokens）**
- 3 處 fetch race conditions（FriendSearch / SharedNoteDetail / CommentSection）
- 殘餘紫色 hex（5 charts）+ 4 處 index.css 舊靛藍 token
- `common.confirm` / `common.saving` 重複 key 移除

**Round 3（backend resilience / PWA / latent bugs）**
- **🔒 Google OAuth bypass 2FA** — 開了 2FA 但 Google login 直接跳過。Mirror password flow partial_token pattern
- `create_friendship` / `remove_friendship` 加 `@transaction.atomic`
- `generate_weekly_summaries` + `send_due_habit_reminders` 加 `bind=True / max_retries / default_retry_delay`，前者加 `connection.close_if_unusable_or_obsolete()` 每 50 user
- iOS Info.plist 補 4 個 UsageDescription（Microphone / SpeechRecognition / Photo / Camera）— App Store blocker
- Service Worker `CACHE_NAME` 寫死 v7 → 自動 build-time 戳上 `<timestamp>-<git-sha>` (`scripts/postbuild-sw.cjs`)

**Round 4（cross-cutting + timezone + WS auth）**
- `ChatConsumer.save_message` 加 transaction.atomic + 處理 conversation 在訊息中被刪
- WebSocket JWT 過期不刷新 → `isAccessTokenExpiring()` 解 `exp` claim，連線前先 `/auth/refresh/`
- `generate_weekly_summaries` 用每位 user 自己的 timezone（不再用 server TIME_ZONE 偏移）

**Round 5（P1/P2 sweep）**
- AchievementsView / AchievementCheckView 加 `permission_classes`
- MoodNote `ordering` 加 `-id` tie-break（pagination 重複 row）
- `get_friend_activity` bulk-fetch streaks（N+1 → 2 queries）
- 全域 `:focus-visible` 規則（一次蓋 a / button / input / select / textarea）
- `AIChatPage` opacity-40 → 60（WCAG AA）
- `NotificationBell` hardcoded slate-400 → `var(--text-secondary)`
- `JournalPage` Trash toggle 加 `aria-pressed`
- `HabitCard` textarea 加 `aria-label`
- `AuthContext.login` 加 `clearCache()`（換 user 不洩漏前人資料）

### 部署 / 基礎設施
- Backend deploy 一次（revision `heartbox-api-00135-lmq`），含 LeaderboardView / cron endpoints / habit reminders
- `CRON_SECRET` 設好 + Cloud Scheduler `habit-reminders`（每 15 min）+ `weekly-summaries`（週一 06:00）已 ENABLED
- Pre-commit hook 修：原本說謊「Backend tests not configured」實際 113 個測試都過 — 換成跑 `manage.py check`（< 1s）

### Mobile (這次 session 結尾)
- Mobile menu 補語言切換（zh-TW / en / ja）— 之前桌面版 dropdown 才有
- 第一次啟動預設淺色（不再跟系統 dark）
- Android adaptive icon 重新生成（`scripts/rebuild-android-icons.py`）— 之前 logo 1.2% padding 被 Samsung squircle / Pixel 圓形 mask 切到，現在 65% 中心 + 20% safe zone
- GitHub secret `VITE_API_URL` 從 Cloud Run 直連 URL 修正為 `https://api.heartbox.tw/api`
- 56 個 project-level Claude Code skills 安裝（addyosmani / anthropics / vercel / netresearch / awesome-skills / nextlevelbuilder）

---

## 🚧 還沒做（按優先排序）

### A. ⚠️ Backend 待 deploy（最迫切）

最後 backend deploy 是 5/9 晚上 revision 00135-lmq。從那之後 backend 累積了 **8 個 commit 沒部署**：

```
bf674ed  P1 sweep（permission_classes / pagination tie-break / friend activity N+1 + migration 0050）
c9425b7  ChatConsumer atomic + WS token refresh 後端側 + weekly_summaries 用 user tz
97185ce  OAuth 2FA bypass + friend transactions + Celery retries
7e8c34d  friends error envelope + HabitAnalytics N+1 / contract drift
```

**動作（在有 Docker Desktop 的機器上）：**
```powershell
cd C:\Users\alan9\OneDrive\Desktop\HeartBox
.\deploy-backend.ps1
```
完成後驗證：
```powershell
curl.exe -I https://api.heartbox.tw/api/friends/leaderboard/  # 應該 401（不是 404）
curl.exe -I https://api.heartbox.tw/api/habits/analytics/      # 401
# 用 demo 登入後打 https://heartbox.tw/habits → 應該看到 habit-mood correlation chart 真的有資料（之前一直是空的）
```

並跑 prod migration（migration 0050 加在這次部署）：
```powershell
# 通常 deploy-backend.ps1 跑 collectstatic 但不跑 migrate；確認 Cloud Run startup 有跑
# 或手動：
$env:DATABASE_URL = "<prod Neon URL>"
.\backend\venv\Scripts\python.exe backend\manage.py migrate api
```

---

### B. 🟠 大型 P0 deferred（需要 infra 配合，不是純程式碼）

| # | 問題 | 規模 | 何時做 |
|---|---|---|---|
| 1 | **NoteForm 沒 retry queue** — 網路斷時遺失打字內容 | 大（IndexedDB queue + sync 邏輯）| 純前端，可單獨做 |
| 2 | **Cloud Run ephemeral filesystem** — 上傳的 attachment 1-2 天蒸發 | 中 | 需 GCS bucket + service account 權限 |
| 3 | **AI 同步呼叫阻塞** weekly_summary + daily_prompt（5-30s；Phase 0b 後改打自架 TAIDE，仍是同步）| 中 | 需部署 Celery worker（目前 Cloud Run 沒 worker，只有 cron）|

---

### C. 🟢 P1/P2 累積（中等 ROI）

**Performance**
- DailySleep / HealthMetric DB 缺 indexes（`user, -created_at` / `user, metric_type`） — 需 migration
- `HealthSummaryView` 用 conditional aggregation 取代迴圈
- Recharts `manualChunks` 拆掉讓 per-route 自動 lazy split

**PWA / Mobile**
- HEIC 圖片轉換（iOS 上傳）
- iOS notch / safe-area 系統性處理
- Health Connect Android <13 用戶看到清楚的「不支援」說明
- Push 權限被 user revoke 時 backend cleanup 端點
- Manifest icons 補 96/144（雖然多數設備不需要）

**A11y**
- LoginPage / DashboardHeader 個別 button focus ring（已有全域 :focus-visible 蓋掉）
- HabitCard textarea 看更深的 contrast tweak

**Backend**
- 51 react-hooks/exhaustive-deps warnings 個案審查（多數是 mount-only loader Bucket B，安全）
- 14 set-state-in-effect warnings（React 19 stricter rule, downgraded to warn）
- Email verification token timeout 太短（共用 `PASSWORD_RESET_TIMEOUT = 15min`）— 可能應該分開

---

### D. 📱 Play Store 上架剩餘（user 要做的）

完整 checklist 見 [`docs/play-store-上架流程剩餘.md`](play-store-上架流程剩餘.md)。

**摘要：**
1. ⏳ Google 身分驗證（被動等）
2. 📸 拍 8 張 auth 頁截圖 × 3 語言（用 emulator + demo 帳號 — 詳見截圖規格表 §3）
3. 📋 Play Console 填表（Main listing × 3 / App content / Health Apps Declaration / Data safety / Content rating）
4. 📦 上傳 AAB → Production track → Save → Review → Start rollout
5. 👁️ 上線後 Crashes & ANRs / Sentry 監控

**Demo 帳號 seed**（要對 prod DB 跑 — `ENCRYPTION_KEY` 必須跟 prod Cloud Run env 一致）：
```powershell
cd backend
$env:DATABASE_URL = "<prod Neon URL>"
$env:ENCRYPTION_KEY = "<prod 一致>"
.\venv\Scripts\python.exe manage.py seed_demo_account
```

---

## 🔄 要遷移到另一台電腦的東西

### 必帶（不在 git）
1. **`backend/.env`** — 含 `DATABASE_URL` / `ENCRYPTION_KEY` / `DJANGO_SECRET_KEY` / `LLM_PROVIDER` / `LLM_SERVER_URL` / `LLM_SERVER_API_KEY` 等（Phase 0b 後改自架 LLM，舊 `OPENAI_API_KEY` 已棄用）
2. **`frontend/.env`** — `VITE_API_URL` 等本地 dev 設定（CI 用 GitHub secrets）
3. **`backend/venv/`** — 重建：`python -m venv venv && venv\Scripts\pip install -r ../requirements.txt`（不需要帶，新機重建即可）
4. **`frontend/node_modules/`** — `npm ci`（同上不帶）
5. **桌面 APK / AAB**（如果還沒上傳到 Play Store）：
   - `Desktop\heartbox-20260510-1305-37755a1-fixed.apk`
   - `Desktop\heartbox-20260510-1305-37755a1-fixed.aab`
   - 最新 mobile build 跑完後桌面會多一個更新版（含 light theme + 新 icon）
6. **桌面 brand source**：`brand/heartbox-icon-flat-{64,128,256,512,1024}.png`（已在 git，遷移免帶）

### 雲端（不需帶，登入即用）
- Cloud Run service `heartbox-api`（asia-east1, project `heartbox-app`）
- Neon PostgreSQL `heartbox-db`
- Cloudflare Pages（frontend 自動 deploy）
- GitHub Actions secrets（all set）— `VITE_API_URL` / `VITE_GOOGLE_CLIENT_ID` / `VITE_VAPID_PUBLIC_KEY` / `ANDROID_KEYSTORE_BASE64` / `ANDROID_KEYSTORE_PASSWORD` / `ANDROID_KEY_ALIAS` / `ANDROID_KEY_PASSWORD` / `CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_ACCOUNT_ID`
- Cloud Scheduler jobs：`habit-reminders`、`weekly-summaries`（asia-east1）

### 新機需安裝
| 工具 | 用途 |
|---|---|
| Git | clone / push |
| Node.js 22+ | frontend |
| Python 3.12 + venv | backend |
| **Docker Desktop** | `deploy-backend.ps1` 必需 |
| gcloud CLI | Cloud Run 操作 + scheduler |
| GitHub CLI (`gh`) | trigger Actions / download artifacts |
| Android Studio | Android emulator + 截圖 |

### Authentication 一次性設定
```powershell
gcloud auth login
gcloud config set project heartbox-app
gcloud auth configure-docker asia-east1-docker.pkg.dev
gh auth login
```

---

## 🔧 新機環境設定（30-60 分鐘）

完整步驟複用 [`工作交接-2026-05-06.md` §A-§D](工作交接-2026-05-06.md)。差異：

- 多裝 Docker Desktop（5/6 那份沒列）
- `winget install Docker.DockerDesktop`，啟動後 `docker version` 確認 client + server 都顯示

---

## 📝 給接手的人 / 自己

### 先做這個
1. 跑 `.\deploy-backend.ps1` 把 8 個未部署 commit 推上 Cloud Run（最高優先 — 修了 OAuth 2FA bypass 等）
2. 拍 Play Store auth 頁截圖（這是上架最後一哩路）
3. Demo 帳號 seed 到 prod

### 之後可選
- 大型 P0 三選一（NoteForm retry / Cloud Run filesystem GCS 遷移 / AI 呼叫 Celery 化）
- 或 P1/P2 sweep 第二輪

### 工具紀錄
- 56 個 Claude Code skills 已 commit 進 `.claude/skills/`（540 個檔案），新機 `git clone` 後直接生效，**不需要重新安裝**。下次 session 可直接 `/find-skills`、`/ui-ux-pro-max`、`/security-scanner`、`/impeccable` 等觸發
- `MEMORY.md` 持久跨 session（`~/.claude/projects/.../memory/`）

---

## 🛠️ Claude Code Skills 來源（跨機 / 升級用）

新機 `git clone` 後 56 個 skills 已存在於 `.claude/skills/`，**無需動作**。但若要重新 install（例如想拉上游最新版本，或在新 repo 建一份），來源 / 重建步驟列在下方。

### 從 5 個 GitHub repos 安裝（24 個 skills）

```bash
# 工作目錄
mkdir -p /tmp/skill-installs && cd /tmp/skill-installs

# 1. Context7 文件查詢（1 個 skill）
git clone --depth=1 https://github.com/netresearch/context7-skill.git
cp -r context7-skill/skills/context7 .claude/skills/

# 2. Anthropic 官方 marketplace（17 個 skills）
git clone --depth=1 https://github.com/anthropics/skills.git anthropics-skills
for s in algorithmic-art brand-guidelines canvas-design claude-api doc-coauthoring docx \
         frontend-design internal-comms mcp-builder pdf pptx skill-creator slack-gif-creator \
         theme-factory web-artifacts-builder webapp-testing xlsx; do
  cp -r anthropics-skills/skills/$s .claude/skills/
done

# 3. Code Review Excellence（1 個）
git clone --depth=1 https://github.com/awesome-skills/code-review-skill.git
mkdir -p .claude/skills/code-review
cp -r code-review-skill/{SKILL.md,README.md,CONTRIBUTING.md,assets,reference,scripts} .claude/skills/code-review/

# 4. UI/UX Pro Max（5 個 sub-skills，namespace `ckm:`）
git clone --depth=1 https://github.com/nextlevelbuilder/ui-ux-pro-max-skill.git
for s in banner-design brand design-system design slides; do
  cp -r ui-ux-pro-max-skill/.claude/skills/$s .claude/skills/
done

# 5. Vercel Web Design Guidelines（1 個）
git clone --depth=1 https://github.com/vercel-labs/agent-skills.git
cp -r agent-skills/skills/web-design-guidelines .claude/skills/
```

### 從 addyosmani/agent-skills 安裝（22 個 skills）

```bash
git clone --depth=1 https://github.com/addyosmani/agent-skills.git addy-skills
cp -r addy-skills/skills/* .claude/skills/
```

22 個：`api-and-interface-design`、`browser-testing-with-devtools`、`ci-cd-and-automation`、`code-review-and-quality`、`code-simplification`、`context-engineering`、`debugging-and-error-recovery`、`deprecation-and-migration`、`documentation-and-adrs`、`doubt-driven-development`、`frontend-ui-engineering`、`git-workflow-and-versioning`、`idea-refine`、`incremental-implementation`、`performance-optimization`、`planning-and-task-breakdown`、`security-and-hardening`、`shipping-and-launch`、`source-driven-development`、`spec-driven-development`、`test-driven-development`、`using-agent-skills`

### 自寫的 stubs（9 個，無上游）

這幾個是這次 session 寫的 project-specific playbook，**沒有 GitHub source**，純粹 commit 在 `.claude/skills/` 內隨 repo 走：

- `find-skills` — 列 / 搜目前可用 skills
- `impeccable` — 多面向協調式全面 audit
- `log-analyzer` — Cloud Run / Sentry / browser console 解析
- `api-documentation-generator` — OpenAPI / docstrings / JSDoc
- `security-scanner` — secrets / OWASP / Django+frontend 特定
- `pipeline-configurator` — GitHub Actions / Husky / Cloud Run
- `dependency-manager` — npm + pip 升級流程
- `refactoring-assistant` — 安全行為保留型重構
- `unit-test-generator` — vitest / Django 測試骨架

### 注意事項

- `.gitignore` 第 49-51 行設了例外 (`.claude/*` 忽略，但 `!.claude/skills/` 例外保留) — 換 repo 時這條 rule 要帶
- Skills 在 Claude Code session **啟動時**讀一次。Repo clone 完開新 session 才會 surface
- Frontmatter `name` 跟資料夾名有時不同（例如 `code-review/SKILL.md` 內 `name: code-review-excellence`，`ui-ux-pro-max-skill` 5 個 sub-skill 用 `ckm:` namespace）— 這沒關係，harness 是按 description 配 user 意圖

### 升級單一 skill（保持新機與上游同步）

```bash
# 例：只升級 context7
cd /tmp && rm -rf context7-skill
git clone --depth=1 https://github.com/netresearch/context7-skill.git
cp -rf context7-skill/skills/context7/* /path/to/.claude/skills/context7/
# 然後 commit + push 變動
```

---

## 📦 Repo 健康指標（可在 PR description / standup 引用）

```
Lint:        Frontend 0 errors / 64 warnings (R-hooks intentional)
Tests:       78/78 vitest, 113/113 Django
i18n:        zh-TW / en / ja 同步 1496 keys, 0 missing
Brand:       0 purple/green drift, 0 invalid Tailwind tokens
Backend:     0 Django check issues, 50 migrations applied
Pagination:  All list endpoints use stable .results unwrap
Race conds:  Major fetch effects guarded
Security:    OAuth 2FA bypass closed, friend transactions atomic
A11y:        Global :focus-visible, aria-labels on icon-only btns
PWA:         SW auto cache-bust per build, ios usage strings present
```
